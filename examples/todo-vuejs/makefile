app_name = todo-vuejs

docker-build:
	#
	# Building web service
	############################################
	docker build -t $(app_name):latest .
	#
	# OK - Building web service
	############################################

docker-run:
	docker-compose up
